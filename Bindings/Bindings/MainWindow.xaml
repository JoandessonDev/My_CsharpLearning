<Window x:Class="Bindings.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bindings"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
   <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Binding Simples - OneWay -->
        <TextBlock Grid.Row="0" Text="Nome:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="txtNome" Text="{Binding Nome}" Margin="0,0,0,15"/>
        
        <!-- Binding TwoWay com UpdateSourceTrigger -->
        <TextBlock Grid.Row="2" Text="Idade:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Text="{Binding Idade, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
        
        <!-- Exibindo os dados com binding -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="{Binding Nome, StringFormat='Nome: {0}'}" FontSize="14" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding Idade, StringFormat='Idade: {0} anos'}" FontSize="14" Margin="0,0,0,10"/>
            
            <!-- Binding com Command -->
            <Button Content="Atualizar Dados" Command="{Binding AtualizarCommand}" 
                    Width="120" HorizontalAlignment="Left" Margin="0,0,0,10"/>
            
            <!-- Lista com binding -->
            <TextBlock Text="Lista de Hobbies:" FontWeight="Bold" Margin="0,10,0,5"/>
            <ListBox ItemsSource="{Binding Hobbies}" Height="80"/>
        </StackPanel>
    </Grid>
</Window>
